import React from 'react'

import { BaseCard } from '../../components/pureStyledComponents/BaseCard'

export const NotAllowed: React.FC = () => {
  return <BaseCard>You are not allowed to access this auction</BaseCard>
}
